# Optional: set only when the browser must call a remote backend directly.
# By default, the user web proxies /backend-api to API_URL.
API_URL=http://127.0.0.1:4000
NEXT_PUBLIC_ADMIN_WEB_URL=http://localhost:3001
NEXT_PUBLIC_AUTH_COOKIE_DOMAIN=
